const config = {
  // REQUIRED
  appName: "ByeSnakes",
  // REQUIRED: a short description of your app for SEO tags (can be overwritten)
  appDescription:
    "ByeSnakes is a simple software that will compare instagram followers and following list to easily catch snakes that don't follow you back.",
  // REQUIRED (no https://, not trialing slash at the end, just the naked domain)
  domainName: "byesnakes.xyx",
};

export default config;
